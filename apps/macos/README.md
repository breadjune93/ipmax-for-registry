# apps/macos

IPMax macOS 앱 (SwiftUI).
작업 시작 전 [`CLAUDE.md`](./CLAUDE.md)를 반드시 읽을 것.

## 다음 단계 (Claude에게 지시 예)

> "apps/macos 에 SwiftUI 앱 + NetworkExtension 타겟(Packet Tunnel Provider) 뼈대를 만들어줘.
> swift-openapi-generator 로 ../../docs/api-contracts/openapi.yaml 에서 클라이언트를 생성."
