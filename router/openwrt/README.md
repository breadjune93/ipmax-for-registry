# router/openwrt

Provider 라우터 펌웨어·에이전트·설정.
작업 시작 전 [`CLAUDE.md`](./CLAUDE.md)를 반드시 읽을 것.

## 다음 단계 (Claude에게 지시 예)

> "router/openwrt/agent 폴더에 Heartbeat 전송 shell 스크립트와 /etc/init.d 서비스 파일 초안을 만들어줘.
> 페이로드는 ../../docs/api-contracts/openapi.yaml 의 /provider/heartbeat 스펙을 따르고, 설정은 UCI로 읽어와."
